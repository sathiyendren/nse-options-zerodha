const accountTypes = {
  PAPER: 'paper',
  REAL: 'real',
};

const tradingTypes = {
  NORMAL: 'normal',
  NEAR_RANGE: 'near-range',
  ANYTIME: 'anytime',
};

module.exports = {
  accountTypes,
  tradingTypes,
};
